// Net-new beta-only workflows (currently empty — all previous entries promoted to stable in 2.17.0)
// Add entries here only when a beta-only workflow doesn't fit any existing stable workflow pattern.
export const betaOnlyWorkflows: any[] = [];
