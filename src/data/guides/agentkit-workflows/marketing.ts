// Marketing workflows — v1 empty (Validation Session 1: Coming soon tab).
// Do not seed appendix K until Marketing kit skill IDs are inventory-confirmed.

import type { AkWorkflow } from "./types";

/** Empty array — UI shows Coming soon empty state (see page chrome in index). */
export const akMarketingWorkflows: AkWorkflow[] = [];

export const akMarketingWorkflowCount = akMarketingWorkflows.length;
