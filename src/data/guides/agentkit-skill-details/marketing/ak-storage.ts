import type { SkillInfographic } from '@/data/guides/how-ck-works';

const data: SkillInfographic = {
  id: 'ak-storage',
  command: '/ak:storage',
  kit: 'marketer',
  header: {
    titleEn: '/ak:storage — S3-compatible object storage',
    titleVi: '/ak:storage — Object storage tương thích S3',
    taglineEn: 'S3-compatible object storage helper for marketing assets across Cloudflare R2, AWS S3, MinIO, Backblaze B2, and DigitalOcean Spaces.',
    taglineVi: 'Trợ lý object storage tương thích S3 cho asset marketing trên Cloudflare R2, AWS S3, MinIO, Backblaze B2 và DigitalOcean Spaces.',
  },
  hardGate: {
    type: 'critical',
    titleEn: 'CREDENTIALS STAY PRIVATE',
    titleVi: 'GIỮ KÍN CREDENTIAL',
    contentEn: 'S3 credentials belong only in the user .env file, must never be logged or exposed, and should use least-privilege bucket-scoped tokens. If not configured or SDK is missing, operations fail gracefully instead of throwing.',
    contentVi: 'Credential S3 chỉ nằm trong file .env của người dùng, không bao giờ log hoặc tiết lộ, và nên dùng token giới hạn theo bucket. Nếu chưa cấu hình hoặc thiếu SDK, thao tác trả lỗi nhẹ nhàng thay vì throw.',
  },
  processFlow: [
    { number: 1, titleEn: 'Identify Operation', titleVi: 'Xác định thao tác', descEn: 'Decide whether the user needs upload, download, list, get public URL, delete, sync, or local-only fallback.', descVi: 'Xác định người dùng cần upload, download, list, lấy public URL, delete, sync hay fallback local-only.' },
    { number: 2, titleEn: 'Check Config', titleVi: 'Kiểm tra config', descEn: 'Look for S3_ENDPOINT, S3_ACCESS_KEY_ID, S3_SECRET_ACCESS_KEY, S3_BUCKET, optional region, and public URL.', descVi: 'Kiểm tra S3_ENDPOINT, S3_ACCESS_KEY_ID, S3_SECRET_ACCESS_KEY, S3_BUCKET, region tùy chọn và public URL.' },
    { number: 3, titleEn: 'Check SDK', titleVi: 'Kiểm tra SDK', descEn: 'Confirm @aws-sdk/client-s3 is installed; otherwise return configured fallback errors.', descVi: 'Xác nhận @aws-sdk/client-s3 đã cài; nếu chưa thì trả lỗi fallback đúng chuẩn.' },
    { number: 4, titleEn: 'Choose Provider', titleVi: 'Chọn provider', descEn: 'Map endpoint shape to Cloudflare R2, AWS S3, MinIO, Backblaze B2, DigitalOcean Spaces, or compatible storage.', descVi: 'Map endpoint sang Cloudflare R2, AWS S3, MinIO, Backblaze B2, DigitalOcean Spaces hoặc storage tương thích.' },
    { number: 5, titleEn: 'Run Client', titleVi: 'Chạy client', descEn: 'Use s3-client.cjs for upload, download, list, getPublicUrl, or remove operations.', descVi: 'Dùng s3-client.cjs cho thao tác upload, download, list, getPublicUrl hoặc remove.' },
    { number: 6, titleEn: 'Return URL or Path', titleVi: 'Trả URL hoặc path', descEn: 'Return public CDN URL when configured, provider URL when available, or local path only on fallback.', descVi: 'Trả CDN public URL nếu có cấu hình, provider URL nếu có, hoặc chỉ path local trong fallback.' },
    { number: 7, titleEn: 'Handle Failure', titleVi: 'Xử lý lỗi', descEn: 'When not configured, all operations return graceful errors and no exceptions are thrown.', descVi: 'Khi chưa cấu hình, mọi thao tác trả lỗi nhẹ nhàng và không throw exception.' },
  ],
  corePrinciplesEn: [
    'Marketing assets can use any S3-compatible backend as long as endpoint, bucket, keys, and region are configured.',
    'The skill returns shareable public URLs when S3_PUBLIC_URL or provider URLs are available.',
    'Fallback behavior is intentional: unconfigured storage should not break local asset workflows.',
    'Credentials are never displayed; use least-privilege bucket-scoped access tokens.',
  ],
  corePrinciplesVi: [
    'Asset marketing có thể dùng mọi backend tương thích S3 nếu endpoint, bucket, key và region được cấu hình.',
    'Skill trả URL public dễ chia sẻ khi có S3_PUBLIC_URL hoặc URL từ provider.',
    'Fallback là hành vi có chủ đích: storage chưa cấu hình không được làm hỏng workflow asset local.',
    'Credential không bao giờ được hiển thị; dùng access token giới hạn quyền theo bucket.',
  ],
  expertiseAreasEn: ['S3-compatible storage', 'Cloudflare R2', 'AWS S3', 'MinIO', 'Backblaze B2', 'DigitalOcean Spaces', 'Asset URLs', 'Graceful fallback'],
  expertiseAreasVi: ['Storage tương thích S3', 'Cloudflare R2', 'AWS S3', 'MinIO', 'Backblaze B2', 'DigitalOcean Spaces', 'URL asset', 'Fallback nhẹ nhàng'],
  invocation: {
    syntax: '/ak:storage [upload|download|list|sync] [file|path] [--bucket <name>]',
    arguments: [
      {
        token: '[file|path]',
        titleEn: 'File or path',
        titleVi: 'File hoặc path',
        descEn: 'Local file, remote object key, or folder path for the selected operation. Required for upload, download, and sync; optional prefix for list.',
        descVi: 'File local, object key remote hoặc path thư mục cho thao tác đã chọn. Bắt buộc với upload, download và sync; prefix tùy chọn với list.',
        required: false,
        exampleCommand: '/ak:storage upload assets/images/launch-hero.png',
        exampleCommandVi: '/ak:storage upload assets/images/launch-hero.png',
      },
    ],
    options: [
      {
        token: '--bucket <name>',
        titleEn: 'Bucket name',
        titleVi: 'Tên bucket',
        descEn: 'Override the configured S3_BUCKET for this operation. Credentials still come from the user .env and are never logged.',
        descVi: 'Override S3_BUCKET đã cấu hình cho thao tác này. Credential vẫn lấy từ .env của user và không bao giờ được log.',
        exampleCommand: '/ak:storage list designs/ --bucket campaign-assets',
        exampleCommandVi: '/ak:storage list designs/ --bucket campaign-assets',
      },
    ],
    subcommands: [
      {
        name: 'upload',
        syntax: '/ak:storage upload [file|path] [--bucket <name>]',
        titleEn: 'Upload',
        titleVi: 'Upload',
        descEn: 'Upload one approved local marketing asset. Inspect destination collisions before overwrite.',
        descVi: 'Upload một asset marketing local đã duyệt. Kiểm tra va chạm đích trước khi ghi đè.',
        arguments: [{ token: '[file|path]', titleEn: 'Local file', titleVi: 'File local', descEn: 'Local file to upload.', descVi: 'File local cần upload.', required: true }],
        options: [{ token: '--bucket <name>', titleEn: 'Bucket name', titleVi: 'Tên bucket', descEn: 'Override S3_BUCKET for this upload.', descVi: 'Override S3_BUCKET cho lượt upload này.' }],
        outcomeEn: 'PutObject result with a constructed URL or a graceful unconfigured fallback; a URL is not proof of public access.',
        outcomeVi: 'Kết quả PutObject kèm URL dựng được hoặc fallback nhẹ khi chưa cấu hình; URL không chứng minh quyền truy cập public.',
        exampleCommand: '/ak:storage upload assets/images/launch-hero.png',
        exampleCommandVi: '/ak:storage upload assets/images/launch-hero.png',
      },
      {
        name: 'download',
        syntax: '/ak:storage download [file|path] [--bucket <name>]',
        titleEn: 'Download',
        titleVi: 'Download',
        descEn: 'Download one approved remote object to a local path, creating parent directories as needed.',
        descVi: 'Download đúng một object remote đã duyệt ra path local, tạo thư mục cha khi cần.',
        arguments: [{ token: '[file|path]', titleEn: 'Remote key or local path', titleVi: 'Key remote hoặc path local', descEn: 'Object key and destination path for the download.', descVi: 'Object key và path đích cho download.', required: true }],
        options: [{ token: '--bucket <name>', titleEn: 'Bucket name', titleVi: 'Tên bucket', descEn: 'Override S3_BUCKET for this download.', descVi: 'Override S3_BUCKET cho lượt download này.' }],
        outcomeEn: 'Local file written from the object bytes, or a missing-key or config error without throwing.',
        outcomeVi: 'File local được ghi từ byte object, hoặc lỗi thiếu key/config mà không throw.',
        exampleCommand: '/ak:storage download designs/pricing-deck.html',
        exampleCommandVi: '/ak:storage download designs/pricing-deck.html',
      },
      {
        name: 'list',
        syntax: '/ak:storage list [file|path] [--bucket <name>]',
        titleEn: 'List',
        titleVi: 'List',
        descEn: 'List remote keys under an optional prefix. Does not upload, download, or delete objects.',
        descVi: 'Liệt kê remote key dưới prefix tùy chọn. Không upload, download hay xóa object.',
        arguments: [{ token: '[file|path]', titleEn: 'Prefix', titleVi: 'Prefix', descEn: 'Optional prefix to bound the list.', descVi: 'Prefix tùy chọn để giới hạn danh sách.' }],
        options: [{ token: '--bucket <name>', titleEn: 'Bucket name', titleVi: 'Tên bucket', descEn: 'Override S3_BUCKET for this list.', descVi: 'Override S3_BUCKET cho lượt list này.' }],
        outcomeEn: 'Bounded object list for that prefix, or a graceful fallback when credentials or the SDK are missing.',
        outcomeVi: 'Danh sách object giới hạn theo prefix, hoặc fallback nhẹ khi thiếu credential hay SDK.',
        exampleCommand: '/ak:storage list designs/',
        exampleCommandVi: '/ak:storage list designs/',
      },
      {
        name: 'sync',
        syntax: '/ak:storage sync [file|path] [--bucket <name>]',
        titleEn: 'Sync',
        titleVi: 'Sync',
        descEn: 'Sync only the requested local files to remote keys. Does not imply remote deletion or changing public access.',
        descVi: 'Sync đúng các file local được yêu cầu sang remote key. Không hàm ý xóa remote hay đổi quyền public.',
        arguments: [{ token: '[file|path]', titleEn: 'Local folder', titleVi: 'Thư mục local', descEn: 'Local folder to sync.', descVi: 'Thư mục local cần sync.', required: true }],
        options: [{ token: '--bucket <name>', titleEn: 'Bucket name', titleVi: 'Tên bucket', descEn: 'Override S3_BUCKET for this sync.', descVi: 'Override S3_BUCKET cho lượt sync này.' }],
        outcomeEn: 'Requested objects mapped and uploaded only after collision inspection; missing credentials keep assets local and report that no upload occurred.',
        outcomeVi: 'Object được yêu cầu được map và upload chỉ sau khi kiểm tra va chạm; thiếu credential thì giữ asset local và báo là chưa upload.',
        exampleCommand: '/ak:storage sync assets/slides/',
        exampleCommandVi: '/ak:storage sync assets/slides/',
      },
    ],
  },
  workflowModes: [
    { flag: 'upload', modeEn: 'Upload one approved local asset.', modeVi: 'Upload một asset local đã duyệt.', research: 'Config + path', redTeam: 'Secret exposure', validation: 'Upload result' },
    { flag: 'download', modeEn: 'Download one approved remote object.', modeVi: 'Download một object remote đã duyệt.', research: 'Object key', redTeam: 'Wrong key', validation: 'Local file' },
    { flag: 'list', modeEn: 'List bounded remote keys.', modeVi: 'Liệt kê remote key có giới hạn.', research: 'Prefix', redTeam: 'Overbroad list', validation: 'Object list' },
    { flag: 'sync', modeEn: 'Sync requested local files only.', modeVi: 'Sync đúng file local được yêu cầu.', research: 'Folder mapping', redTeam: 'Unexpected writes', validation: 'Requested objects' },
  ],
  promptExamples: [
    {
      labelEn: 'Default inspect',
      labelVi: 'Kiểm tra mặc định',
      command: '/ak:storage',
      commandVi: '/ak:storage',
      whenEn: 'You need to resolve the authorized operation, bucket, and whether credentials exist before touching objects.',
      whenVi: 'Cần chốt thao tác được phép, bucket và việc đã có credential chưa trước khi đụng object.',
      expectedEn: 'Resolves configuration and SDK presence, reports local-only fallback when unconfigured, and does not invent slash flags beyond upload, download, list, sync, and --bucket.',
      expectedVi: 'Chốt cấu hình và sự hiện diện của SDK, báo fallback chỉ local khi chưa cấu hình, và không bịa slash flag ngoài upload, download, list, sync và --bucket.',
      recommended: true,
    },
    {
      labelEn: 'Upload asset',
      labelVi: 'Upload asset',
      command: '/ak:storage upload assets/images/launch-hero.png',
      commandVi: '/ak:storage upload assets/images/launch-hero.png',
      whenEn: 'A generated marketing asset should be hosted remotely after approval.',
      whenVi: 'Một asset marketing đã tạo cần được host remote sau khi duyệt.',
      expectedEn: 'Reads the local file, inspects destination collisions, sends PutObject when approved, and returns a constructed URL or a graceful unconfigured fallback instead of throwing.',
      expectedVi: 'Đọc file local, kiểm tra va chạm đích, gửi PutObject khi đã duyệt, rồi trả URL dựng được hoặc fallback nhẹ khi chưa cấu hình, không throw.',
    },
    {
      labelEn: 'Download object',
      labelVi: 'Download object',
      command: '/ak:storage download designs/pricing-deck.html',
      commandVi: '/ak:storage download designs/pricing-deck.html',
      whenEn: 'One approved remote object should be written to a local path.',
      whenVi: 'Một object remote đã duyệt cần được ghi ra path local.',
      expectedEn: 'Creates parent directories, writes the object bytes to the requested local path, and reports a missing-key or config error without crashing.',
      expectedVi: 'Tạo thư mục cha, ghi byte object ra path local đã nêu, và báo thiếu key hoặc config mà không crash.',
    },
    {
      labelEn: 'List prefix',
      labelVi: 'List prefix',
      command: '/ak:storage list designs/',
      commandVi: '/ak:storage list designs/',
      whenEn: 'You need remote asset keys under a prefix without uploading or deleting anything.',
      whenVi: 'Cần remote asset key dưới một prefix mà không upload hay xóa gì.',
      expectedEn: 'Lists a bounded object set for that prefix, or a fallback status when credentials or the SDK are missing, without changing public access.',
      expectedVi: 'Liệt kê tập object giới hạn theo prefix, hoặc trạng thái fallback khi thiếu credential hay SDK, không đổi quyền public.',
    },
    {
      labelEn: 'Sync folder',
      labelVi: 'Sync thư mục',
      command: '/ak:storage sync assets/slides/',
      commandVi: '/ak:storage sync assets/slides/',
      whenEn: 'A local asset folder should be mirrored for only the requested objects.',
      whenVi: 'Một thư mục asset local cần được mirror đúng các object được yêu cầu.',
      expectedEn: 'Maps requested local files to remote keys, inspects collisions, does not delete remote objects, and keeps assets local if credentials are missing.',
      expectedVi: 'Map file local được yêu cầu sang remote key, kiểm tra va chạm, không xóa object remote, và giữ asset local nếu thiếu credential.',
    },
    {
      labelEn: 'List with bucket',
      labelVi: 'List kèm bucket',
      command: '/ak:storage list designs/ --bucket campaign-assets',
      commandVi: '/ak:storage list designs/ --bucket campaign-assets',
      whenEn: 'The list must target a named bucket instead of the default S3_BUCKET.',
      whenVi: 'Lượt list phải nhắm một bucket đã nêu thay vì S3_BUCKET mặc định.',
      expectedEn: 'Overrides the bucket for that list only, still loads credentials from the user .env without logging them, and returns a bounded prefix listing or a graceful config error.',
      expectedVi: 'Override bucket cho đúng lượt list đó, vẫn nạp credential từ .env của user mà không log, rồi trả listing prefix có giới hạn hoặc lỗi config nhẹ.',
    },
  ],
};

export default data;
