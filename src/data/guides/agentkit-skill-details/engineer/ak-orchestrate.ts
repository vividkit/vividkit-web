import type { SkillInfographic } from '@/data/guides/how-ck-works';

const data: SkillInfographic = {
  id: 'ak-orchestrate',
  command: '/ak:orchestrate',
  kit: 'engineer',
  header: { titleEn: '/ak:orchestrate — Coordinate coding-agent jobs', titleVi: '/ak:orchestrate — Điều phối job coding-agent', taglineEn: 'Coordinate staged or parallel coding-agent jobs with live runtime routing, isolated ownership, captured state, safety gates, and independent arbiter review.', taglineVi: 'Điều phối job coding-agent theo stage hoặc song song với routing runtime trực tiếp, quyền sở hữu tách biệt, state được ghi lại, safety gate và arbiter độc lập.' },
  hardGate: { type: 'critical', titleEn: 'LIVE EVIDENCE OR STOP', titleVi: 'PHẢI CÓ BẰNG CHỨNG SỐNG HOẶC DỪNG', contentEn: 'Runtime catalogs, models, flags, and permissions drift. Every route must be verified at execution time; missing controls, secrets in prompts, unapproved destructive work, or reference disagreement must block rather than silently downgrade.', contentVi: 'Danh sách runtime, model, flag và quyền có thể thay đổi. Mọi route phải được xác minh lúc chạy; thiếu kiểm soát, có secret trong prompt, hành động phá hủy chưa duyệt hoặc tài liệu mâu thuẫn thì phải chặn, không được tự hạ chuẩn.' },
  processFlow: [
    { number: 1, titleEn: 'Intake outcome', titleVi: 'Nhận mục tiêu', descEn: 'Clarify outcome, constraints, non-goals, workspace root, dependencies, destructive intent, external effects, and expected evidence.', descVi: 'Làm rõ kết quả mong muốn, ràng buộc, non-goal, workspace root, phụ thuộc, ý định phá hủy, tác động bên ngoài và bằng chứng cần có.' },
    { number: 2, titleEn: 'Build job graph', titleVi: 'Dựng graph job', descEn: 'Convert the request or YAML spec into jobs with task, cwd, timeout, expected output, ownership, dependencies, and isolation.', descVi: 'Chuyển yêu cầu hoặc YAML spec thành các job có task, cwd, timeout, output mong đợi, ownership, dependency và isolation.' },
    { number: 3, titleEn: 'Discover runtimes', titleVi: 'Phát hiện runtime', descEn: 'Build a live inventory, profile each candidate, and record verified permissions, capture behavior, budgets, and enablement.', descVi: 'Tạo inventory runtime trực tiếp, profile từng lựa chọn và ghi quyền, capture, budget, trạng thái enable đã xác minh.' },
    { number: 4, titleEn: 'Route by risk', titleVi: 'Route theo rủi ro', descEn: 'Use the routing authority to select runtime, model or internal agent, capability tier, risk tier, controls, and fallback reason.', descVi: 'Dùng tài liệu routing để chọn runtime, model hoặc agent nội bộ, capability tier, risk tier, kiểm soát và lý do fallback.' },
    { number: 5, titleEn: 'Apply safety gate', titleVi: 'Áp safety gate', descEn: 'Confirm cwd, allowed files, writable roots, side effects, approvals, worktree needs, and timeout boundaries before dispatch.', descVi: 'Xác nhận cwd, file được phép chạm, root được ghi, tác động phụ, phê duyệt, nhu cầu worktree và giới hạn timeout trước khi dispatch.' },
    { number: 6, titleEn: 'Dispatch and capture', titleVi: 'Dispatch và capture', descEn: 'Start independent jobs concurrently, update state.json, capture command/output/artifacts/status, and fail on unknown flags, prompts, or timeouts.', descVi: 'Chạy job độc lập song song, cập nhật state.json, ghi command/output/artifact/status và fail khi gặp flag lạ, permission prompt hoặc timeout.' },
    { number: 7, titleEn: 'Arbiter review', titleVi: 'Arbiter review', descEn: 'Use a separate judgment route to compare outputs against expected artifacts, checks, claims, risk floors, and unresolved questions.', descVi: 'Dùng route đánh giá riêng để so output với artifact kỳ vọng, check, claim, mức rủi ro tối thiểu và câu hỏi còn mở.' },
    { number: 8, titleEn: 'Report and resume', titleVi: 'Báo cáo và resume', descEn: 'Write report.md, metrics, artifacts, worktree diffs, failures, and resume state without hiding or relabeling incomplete work.', descVi: 'Ghi report.md, metrics, artifact, diff worktree, lỗi và state resume; không che giấu hoặc đổi nhãn công việc chưa hoàn tất.' },
  ],
  corePrinciplesEn: ['Prefer direct single-agent work when orchestration adds no value', 'Same-stage jobs run concurrently only with non-overlapping ownership', 'Worktrees prevent edit collisions but are not OS sandboxes', 'Arbiter review is required before handoff claims'],
  corePrinciplesVi: ['Ưu tiên một agent trực tiếp khi orchestration không tạo giá trị', 'Job cùng stage chỉ chạy song song khi ownership không chồng lấn', 'Worktree tránh đụng edit nhưng không phải sandbox hệ điều hành', 'Phải có arbiter review trước khi tuyên bố bàn giao'],
  workflowModes: [
    { flag: '--internal', modeEn: 'Prefer in-session subagents for jobs without explicit runtime pins, while still allowing verified CLI fallback when required.', modeVi: 'Ưu tiên subagent trong session cho job chưa pin runtime, nhưng vẫn cho fallback CLI đã xác minh khi cần.', research: 'live', redTeam: 'arbiter', validation: 'required' },
    { flag: '--yes', modeEn: 'Treat exact pre-approved destructive/external scope as confirmed; it does not waive safety checks or route evidence.', modeVi: 'Xem phạm vi phá hủy/bên ngoài đã duyệt chính xác là được xác nhận; không bỏ qua safety check hoặc bằng chứng route.', research: 'live', redTeam: 'arbiter', validation: 'required' },
    { flag: '--resume <run-dir>', modeEn: 'Reload jobs.yaml and state.json, preserve successful outputs, revalidate routes, and redispatch only interrupted jobs.', modeVi: 'Nạp lại jobs.yaml và state.json, giữ output đã thành công, xác minh lại route và chỉ dispatch lại job bị gián đoạn.', research: 'revalidate', redTeam: 'arbiter', validation: 'required' },
  ],
  composableFlagsEn: '--internal is a routing preference. --yes only covers the exact already-approved scope. --resume reuses captured state rather than starting over.',
  composableFlagsVi: '--internal là ưu tiên routing. --yes chỉ áp dụng cho phạm vi đã được duyệt chính xác. --resume dùng lại state đã capture thay vì làm lại từ đầu.',
  promptExamples: [
    { labelEn: 'Free-form orchestration', labelVi: 'Điều phối tự do', command: '/ak:orchestrate "research three implementation options and compare them"', whenEn: 'Use when independent research or implementation jobs can run in stages.', whenVi: 'Dùng khi có thể chia research hoặc implementation thành các job độc lập theo stage.', expectedEn: 'A captured job spec, routed jobs, arbiter verdict, and report.', expectedVi: 'Có job spec được capture, job được route, arbiter verdict và report.', recommended: true },
    { labelEn: 'YAML spec', labelVi: 'Spec YAML', command: '/ak:orchestrate plans/orchestrate-jobs.yaml', whenEn: 'Use for repeatable job graphs with explicit dependencies and ownership.', whenVi: 'Dùng cho graph job lặp lại được, có dependency và ownership rõ.', expectedEn: 'Execution follows the YAML job spec and records state under reports.', expectedVi: 'Việc chạy bám theo YAML job spec và ghi state trong reports.' },
    { labelEn: 'Internal preference', labelVi: 'Ưu tiên nội bộ', command: '/ak:orchestrate "compare the auth options" --internal', whenEn: 'Use when in-session subagents are preferred but live routing can still choose a safer fallback.', whenVi: 'Dùng khi ưu tiên subagent trong session nhưng routing trực tiếp vẫn có thể chọn fallback an toàn hơn.', expectedEn: 'Internal-first route evaluation with recorded fallback reasons if any.', expectedVi: 'Đánh giá route ưu tiên internal, ghi rõ lý do fallback nếu có.' },
    { labelEn: 'Resume run', labelVi: 'Tiếp tục run', command: '/ak:orchestrate --resume plans/reports/orchestrate-20260829', whenEn: 'Use after interruption to continue from captured run state.', whenVi: 'Dùng sau khi bị gián đoạn để tiếp tục từ state đã ghi.', expectedEn: 'Interrupted jobs revalidated and resumed without duplicating successful work.', expectedVi: 'Job bị gián đoạn được xác minh lại và resume, không lặp lại phần đã thành công.' },
  ],
  skillStack: [{ name: 'model-routing.md', type: 'tool' }, { name: 'runtime-matrix.md', type: 'tool' }, { name: 'harness-profiles.md', type: 'tool' }, { name: 'internal-routing.md', type: 'tool' }, { name: 'worktrees', type: 'tool' }],
  reportOutput: { titleEn: 'Orchestrate Report', titleVi: 'Báo cáo orchestrate', patternEn: 'plans/reports/orchestrate-<timestamp>/report.md', patternVi: 'plans/reports/orchestrate-<timestamp>/report.md', locationEn: 'Captured run directory with jobs.yaml, runtimes.json, state.json, artifacts, status, and worktrees', locationVi: 'Thư mục run có jobs.yaml, runtimes.json, state.json, artifact, status và worktree', descEn: 'Final result lists spec, report path, job counts, arbiter status, checks, artifacts, unresolved questions, and integration-ready diffs.', descVi: 'Kết quả cuối nêu spec, đường dẫn report, số job, trạng thái arbiter, check, artifact, câu hỏi còn mở và diff sẵn sàng tích hợp.' },
};

export default data;
