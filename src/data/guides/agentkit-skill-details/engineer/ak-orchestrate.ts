import type { SkillInfographic } from '@/data/guides/how-ck-works';

const data: SkillInfographic = {
  id: 'ak-orchestrate',
  command: '/ak:orchestrate',
  kit: 'engineer',
  header: { titleEn: '/ak:orchestrate — Coordinate captured agent runs', titleVi: '/ak:orchestrate — Điều phối run agent có ghi nhận', taglineEn: 'Coordinate staged or parallel jobs across live-verified coding-agent runtimes and in-session subagents with risk-based routing, worktree-isolated writes, resumable state, capture, safety gates, and independent arbiter review.', taglineVi: 'Điều phối job theo stage hoặc song song qua runtime coding-agent được xác minh trực tiếp và subagent trong session, kèm routing theo rủi ro, write tách bằng worktree, state có thể resume, capture, safety gate và arbiter độc lập.' },
  hardGate: { type: 'critical', titleEn: 'LIVE ROUTE EVIDENCE OR STOP', titleVi: 'PHẢI CÓ BẰNG CHỨNG ROUTE TRỰC TIẾP HOẶC DỪNG', contentEn: 'Runtime, provider, model, alias, flag, and agent availability drift. Every route must be resolved from execution-time evidence; secrets in prompts, missing controls, unapproved destructive or external work, unsupported candidates, or reference disagreement block the run.', contentVi: 'Runtime, provider, model, alias, flag và agent có thể thay đổi. Mọi route phải được phân giải từ bằng chứng lúc chạy; secret trong prompt, thiếu kiểm soát, việc phá hủy hoặc tác động ngoài chưa duyệt, candidate không đạt chuẩn hoặc tài liệu tham chiếu mâu thuẫn đều chặn run.' },
  processFlow: [
    { number: 1, titleEn: 'Brainstorm and intake', titleVi: 'Brainstorm và nhận yêu cầu', descEn: 'Clarify outcome, constraints, non-goals, acceptance evidence, workspace root, dependencies, destructive or external intent, expected outputs, and runtime constraints.', descVi: 'Làm rõ kết quả mong muốn, ràng buộc, non-goal, bằng chứng chấp nhận, workspace root, phụ thuộc, ý định phá hủy hoặc tác động ngoài, output kỳ vọng và ràng buộc runtime.' },
    { number: 2, titleEn: 'Build job graph', titleVi: 'Dựng graph job', descEn: 'Convert the accepted outcome into jobs with task, cwd, timeout, expected output, file ownership, dependencies, importance, isolation, concurrency, and explicit skill instructions.', descVi: 'Chuyển outcome đã nhận thành các job có task, cwd, timeout, output kỳ vọng, ownership file, dependency, importance, isolation, concurrency và chỉ dẫn skill rõ ràng.' },
    { number: 3, titleEn: 'Discover, profile, route', titleVi: 'Phát hiện, profile, route', descEn: 'Build a live runtime inventory, profile candidates, invoke model-routing, and record selected runtime, model or agent, capability tier, risk tier, controls, evidence, and fallback reason.', descVi: 'Tạo inventory runtime trực tiếp, profile candidate, gọi model-routing và ghi runtime được chọn, model hoặc agent, capability tier, risk tier, kiểm soát, bằng chứng và lý do fallback.' },
    { number: 4, titleEn: 'Apply safety gate', titleVi: 'Áp safety gate', descEn: 'Confirm cwd, allowed files, writable roots, side effects, least-privilege controls, approval for destructive or external actions, worktree needs, and external timeouts before dispatch.', descVi: 'Xác nhận cwd, file được phép chạm, root được ghi, tác động phụ, kiểm soát least-privilege, phê duyệt cho hành động phá hủy hoặc tác động ngoài, nhu cầu worktree và timeout ngoài trước khi dispatch.' },
    { number: 5, titleEn: 'Dispatch and capture', titleVi: 'Dispatch và capture', descEn: 'Create required worktrees, start independent jobs up to concurrency, update state.json on transitions, use ak orchestrate lifecycle delegation where supported, and capture redacted outputs and artifacts.', descVi: 'Tạo worktree cần thiết, chạy job độc lập theo giới hạn concurrency, cập nhật state.json khi chuyển trạng thái, dùng cơ chế lifecycle của ak orchestrate nơi được hỗ trợ và capture output/artifact đã redacted.' },
    { number: 6, titleEn: 'Run arbiter review', titleVi: 'Chạy arbiter review', descEn: 'After runnable jobs settle, use a separate C3 judgment route to compare outputs with expected artifacts, checks, original intent, route floors, safety gaps, contradictions, and unresolved questions.', descVi: 'Sau khi các job runnable đã settle, dùng route đánh giá C3 riêng để so output với artifact kỳ vọng, check, ý định ban đầu, route floor, safety gap, mâu thuẫn và câu hỏi còn mở.' },
    { number: 7, titleEn: 'Report captured result', titleVi: 'Báo cáo kết quả đã capture', descEn: 'Write report.md, metrics history, job status, resolved routes, artifacts, errors, arbiter verdict, checks, reproduction commands, worktree diffs awaiting integration, and unresolved questions.', descVi: 'Ghi report.md, lịch sử metrics, trạng thái job, route đã phân giải, artifact, lỗi, verdict của arbiter, check, lệnh tái hiện, diff worktree chờ tích hợp và câu hỏi còn mở.' },
  ],
  corePrinciplesEn: ['Prefer direct single-agent work when orchestration adds no useful parallelism, staged dependency, runtime diversity, or arbiter value', 'Resolve every runtime, model, alias, flag, and agent from live execution-time evidence', 'Same-stage jobs run concurrently only with non-overlapping ownership and outputs', 'Worktrees prevent edit collisions but are not OS sandboxes', 'Arbiter review is required before handoff claims'],
  corePrinciplesVi: ['Ưu tiên một agent trực tiếp khi orchestration không tạo thêm parallelism, dependency theo stage, đa dạng runtime hoặc giá trị arbiter', 'Phân giải mọi runtime, model, alias, flag và agent từ bằng chứng trực tiếp lúc chạy', 'Job cùng stage chỉ chạy song song khi ownership và output không chồng lấn', 'Worktree tránh đụng edit nhưng không phải sandbox hệ điều hành', 'Phải có arbiter review trước khi tuyên bố bàn giao'],
  workflowModes: [
    { flag: '--internal', modeEn: 'Prefer in-session subagents for jobs without explicit runtime pins, while still allowing live-verified CLI fallback when capability, model selection, or isolation requires it.', modeVi: 'Ưu tiên subagent trong session cho job chưa pin runtime, nhưng vẫn cho fallback CLI đã xác minh trực tiếp khi capability, chọn model hoặc isolation yêu cầu.', research: 'live', redTeam: 'arbiter', validation: 'required' },
    { flag: '--yes', modeEn: 'Treat only the exact pre-approved destructive or external scope as confirmed; it never waives live route evidence, least-privilege controls, or safety gates.', modeVi: 'Chỉ xem phạm vi phá hủy hoặc tác động ngoài đã duyệt chính xác là được xác nhận; không bao giờ bỏ qua bằng chứng route trực tiếp, kiểm soát least-privilege hoặc safety gate.', research: 'live', redTeam: 'arbiter', validation: 'required' },
    { flag: '--resume <run-dir>', modeEn: 'Reload the captured jobs.yaml and state.json, preserve successful outputs and prior attempts, revalidate live routes, and redispatch only interrupted jobs.', modeVi: 'Nạp lại jobs.yaml và state.json đã capture, giữ output thành công và attempt cũ, xác minh lại route trực tiếp và chỉ dispatch lại job bị gián đoạn.', research: 'revalidate', redTeam: 'arbiter', validation: 'required' },
  ],
  composableFlagsEn: '--internal is a routing preference. --yes covers only exact already-approved destructive or external scope. --resume uses captured state rather than starting over.',
  composableFlagsVi: '--internal là ưu tiên routing. --yes chỉ áp dụng cho phạm vi phá hủy hoặc tác động ngoài đã được duyệt chính xác. --resume dùng state đã capture thay vì làm lại từ đầu.',
  promptExamples: [
    { labelEn: 'Free-form staged run', labelVi: 'Run theo stage từ yêu cầu tự do', command: '/ak:orchestrate "research three implementation options and compare them"', whenEn: 'Use when independent research or implementation jobs can run in stages and need captured evidence plus arbiter review before handoff.', whenVi: 'Dùng khi các job research hoặc implementation độc lập có thể chạy theo stage và cần capture bằng chứng kèm arbiter review trước khi bàn giao.', expectedEn: 'Creates a temporary jobs.yaml under plans/reports/orchestrate-<timestamp>/, live-routes each job, captures state and artifacts, runs the arbiter checklist, and writes report.md.', expectedVi: 'Tạo jobs.yaml tạm trong plans/reports/orchestrate-<timestamp>/, route trực tiếp từng job, capture state và artifact, chạy checklist arbiter và ghi report.md.', recommended: true },
    { labelEn: 'Repeatable YAML spec', labelVi: 'Spec YAML lặp lại được', command: '/ak:orchestrate plans/orchestrate-jobs.yaml', whenEn: 'Use for repeatable job graphs with explicit dependencies, expected outputs, ownership, isolation, checks, and concurrency.', whenVi: 'Dùng cho graph job lặp lại được với dependency, output kỳ vọng, ownership, isolation, check và concurrency rõ ràng.', expectedEn: 'Reads the YAML job spec, validates stages and ownership, discovers live runtimes, records runtimes.json and state.json, executes runnable jobs, and reports arbiter findings.', expectedVi: 'Đọc job spec YAML, xác minh stage và ownership, phát hiện runtime trực tiếp, ghi runtimes.json và state.json, chạy các job runnable và báo cáo phát hiện của arbiter.' },
    { labelEn: 'Internal-first routing', labelVi: 'Routing ưu tiên nội bộ', command: '/ak:orchestrate "compare the auth options" --internal', whenEn: 'Use when in-session subagents should be considered first, but a live-verified CLI fallback may still be required for model control or stronger isolation.', whenVi: 'Dùng khi nên xét subagent trong session trước, nhưng fallback CLI đã xác minh trực tiếp vẫn có thể cần cho kiểm soát model hoặc isolation mạnh hơn.', expectedEn: 'Profiles internal dispatch and CLI candidates, records the resolved agent or fallback runtime in status.json, captures internal result.md without fake subprocess fields, and blocks unsafe downgrades.', expectedVi: 'Profile dispatch nội bộ và candidate CLI, ghi agent hoặc runtime fallback đã phân giải trong status.json, capture result.md nội bộ không giả lập field subprocess và chặn việc hạ chuẩn thiếu an toàn.' },
    { labelEn: 'Resume interrupted run', labelVi: 'Resume run bị gián đoạn', command: '/ak:orchestrate --resume plans/reports/orchestrate-20260829', whenEn: 'Use after an interruption when jobs.yaml and state.json already exist and successful outputs or prior attempts must be preserved.', whenVi: 'Dùng sau gián đoạn khi jobs.yaml và state.json đã tồn tại và cần giữ output thành công hoặc attempt trước đó.', expectedEn: 'Reloads the run directory, reconnects delegated ak orchestrate jobs where possible, revalidates live routes, preserves completed work, redispatches only interrupted jobs, and updates report.md.', expectedVi: 'Nạp lại thư mục run, reconnect các job đã giao cho ak orchestrate khi có thể, xác minh lại route trực tiếp, giữ phần đã xong, chỉ dispatch lại job bị gián đoạn và cập nhật report.md.' },
  ],
  skillStack: [{ name: 'model-routing.md', type: 'tool' }, { name: 'runtime-matrix.md', type: 'tool' }, { name: 'harness-profiles.md', type: 'tool' }, { name: 'internal-routing.md', type: 'tool' }, { name: 'job-spec.md', type: 'tool' }],
  reportOutput: { titleEn: 'Orchestrate Result', titleVi: 'Kết quả orchestrate', patternEn: 'plans/reports/orchestrate-<timestamp>/report.md', patternVi: 'plans/reports/orchestrate-<timestamp>/report.md', locationEn: 'Captured run directory with jobs.yaml, runtimes.json, state.json, report.md, per-job status, artifacts, attempts, and worktrees when used', locationVi: 'Thư mục run đã capture gồm jobs.yaml, runtimes.json, state.json, report.md, status từng job, artifact, attempt và worktree khi dùng', descEn: 'Final report lists spec, job counts, resolved runtime/model/agent routes, capability and risk tiers, artifacts, errors, arbiter verdict, checks, reproduction commands, worktree diffs awaiting integration, and unresolved questions.', descVi: 'Báo cáo cuối nêu spec, số job, route runtime/model/agent đã phân giải, capability và risk tier, artifact, lỗi, verdict của arbiter, check, lệnh tái hiện, diff worktree chờ tích hợp và câu hỏi còn mở.' },
};

export default data;
